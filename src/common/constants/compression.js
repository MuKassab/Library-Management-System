// Value MUST be from 0 - 9 where 0 is the faster but largest in size
export const COMPRESSION_LEVEL = 6;

// If response size is greater than this threshold then compress it
export const COMPRESSION_THRESHOLD = 45 * 1024;
